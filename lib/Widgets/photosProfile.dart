// ignore_for_file: file_names

import 'package:flutter/material.dart';

class PhotosProfile extends StatelessWidget {
  const PhotosProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('data');
  }
}
